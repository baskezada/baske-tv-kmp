# Copiá este archivo a `keystore.properties` (en la raíz del proyecto) y completá
# con TU clave de subida — la MISMA con la que se publicó tv.baske.app en Play,
# si no Play rechaza el update por firma distinta.
#
# `keystore.properties` está en .gitignore: no se sube al repo.

storeFile=C:\\Users\\baske\\keys\\baske-upload.jks
storePassword=CAMBIAR
keyAlias=CAMBIAR
keyPassword=CAMBIAR
