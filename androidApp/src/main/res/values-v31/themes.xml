<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash de Android 12+: fondo negro de marca + león cian con padding
         propio (splash_icon), en vez de que el sistema use el ícono a full. -->
    <style name="Theme.BaskeTV" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowSplashScreenBackground">@android:color/black</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_lion_splash</item>
    </style>
</resources>
